class UI_button::

enum button_statez{BTN_idle =0, BTN_hover, BTN_prez};

Private :
short unigned


bool pressed;
bool hover;


void Button::update



Butten

enum button_states{BTN_IDLE = 0, BTN_HOVER, BTN_ACTIVE};

class sqr_basic_Button
{
  private:
      short unsigned BTN_state;
      short unsigned BT_id;

      sf::RectangleShape BTN_rec_shape;

      sf::Font* prt_font;
      sf::Text BTN_text;

        sf::Color textIdleColor;
    		sf::Color textHoverColor;
    		sf::Color textActiveColor;

    		sf::Texture idleColor;
    		sf::Texture hoverColor;
    		sf::Texture activeColor;

    		sf::Color outlineIdleColor;
    		sf::Color outlineHoverColor;
        sf::Color outlineActiveColor;

    public:

      Button(float x, float y, float width, float height,


      )




}
